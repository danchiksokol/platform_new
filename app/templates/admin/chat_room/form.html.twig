{% extends 'admin/base.html.twig' %}

{% block body %}
    {{ form_start(chatRoomForm) }}
    <div class="container">
        <div class="row">
            <table class="table table-hover table-bordered">
                <tbody>
                <tr>
                    <td>Название</td>
                    <td>{{ form_row(chatRoomForm.name) }}</td>
                </tr>
                <tr>
                    <td>Трансляция</td>
                    <td>{{ form_row(chatRoomForm.broadcast) }}</td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="row">
            <div class="col-md-6">{{ form_row(chatRoomForm.save) }}</div>
        </div>
    </div>
    {{ form_end(chatRoomForm) }}
{% endblock %}
